#!/usr/bin/env ruby

require 'kramdown-converter-indesign'

icml = nil

ARGV.each do |arg|
  input = File.read(arg).strip
  input += "\n\n" unless arg == ARGV[-1]
  icml = Kramdown::Document.new(input, indesign_append_to_icml: icml).to_indesign
end

print icml